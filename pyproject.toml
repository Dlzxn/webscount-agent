[project]
name = "webscount-agent"
version = "0.1.0"
description = "AI browser-automation agent with MCP + Anthropic"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "fastapi>=0.139.0",
    "mcp>=1.28.1",
    "playwright>=1.61.0",
    "python-dotenv>=1.0.0",
    "uvicorn[standard]>=0.30.0",
]
